# 硬件PM工作台 · 每日内容生成管线说明（继承用）

> 用途：本工作台 App（手机上运行，部署于 `app.codebuddy.work`）的「新闻 / 收盘复盘」两个页面，
> 依赖 App 之外的**每日 AI 内容生成管线**写入 `data/*.json`。本文档记录这些管线的触发、产出结构、与线上部署的关系，
> 以便换电脑/云端管线出问题时可接管、升级。

## 一、两条管线总览

| # | 任务 | 产出文件 | 触发时间(原云端) | 消费页面 | 本机备用自动化 |
|---|------|----------|------------------|----------|----------------|
| ① | 每日市场资讯 | `data/news.json` | 约 08:50 | 新闻 | `[备用] 每日市场资讯生成 news.json`（当前 ACTIVE） |
| ② | 每日收盘复盘 | `data/review.json` | 每日 14:00（代码注释 `automation runs at 2 PM`） | 收盘复盘 | `[备用] 每日收盘复盘生成 review.json`（当前 ACTIVE） |

> 注：`[备用] 每日视频拆解生成 videos.json` 已于 2026-07-30 应要求删除；视频拆解页面改由线上管线直接供数。

现状（2026-07-30）：`news.json` 的 `generatedAt` 为今日 08:50，数据新鲜 → **原云端管线仍在正常运行**。
本机这 2 条备用自动化已被用户启用（ACTIVE），作为原云端管线的对等/备用。

## 二、各文件 JSON 结构（严格匹配，App 按此解析）

> 🔤 **写入铁律（2026-08-04 事故）**：中文长文本字段（`marketOverview` / `summary` / `impact` / `note` / `opportunities` / `risks`）里**绝对不要出现裸 ASCII 双引号** `"`。08-03 复盘写了 `典型"指数跌、个股涨"分化` → 直接产出**非法 JSON**，App 的 `Promise.allSettled` 静默吞掉，「投资理财→复盘」整页无数据，且**巡检 MD5 比对发现不了**（线上本机一样坏）。
> 需要引号时一律用中文引号 “ ” 或书名号。**写完必须跑 `python3 -c "import json;json.load(open('data/xxx.json'))"` 校验通过才算完成。**

### news.json
> ✅ **2026-08-04 巡检校正**：以下为**现行真实结构**（旧版文档写的 8 分类 + `impact` 字段已废弃，
> 全库 `grep '\.impact'` **0 处消费**，勿再据旧 schema 判「缺字段」误报）。现为 **4 分类**，条目字段含 `pubTime`（可空）。

```json
{
  "generatedAt": "2026-08-04T13:05:44+08:00",
  "categories": [
    {"key":"official","label":"官媒热点","icon":"🏛️"},
    {"key":"hardware","label":"消费电子","icon":"📱"},
    {"key":"ai","label":"AI硬件","icon":"🤖"},
    {"key":"tech","label":"科技前沿","icon":"💡"}
  ],
  "items": [
    {"id":"n1","priority":5,"category":"official","title":"...","summary":"...","source":"...","url":"...","pubTime":"","tags":["..."]}
  ]
}
```
- 必需字段：`id`(唯一) / `priority`(2-5) / `category`(须在 categories 内) / `title` / `summary` / `source` / `url` / `tags`。
- `pubTime` 允许为空串，app.js:2046 用 `if (n.pubTime)` 守卫，不显示时间戳而已，**非故障**。
- app.js 渲染分类标签取自本文件自带的 `categories` 数组；首页/简报统计把 `hardware|ai|tech` 三类合并为「硬件」口径（app.js:1727、2120）。

### review.json
```json
{
  "generatedAt": "2026-07-30T14:00:00+08:00",
  "marketOverview": "今日A股...",
  "sectors": [{"name":"半导体","change":-1.2,"note":"..."}],
  "indices": [{"name":"上证指数","value":3828.47,"change":0.4}],
  "opportunities": "...",
  "risks": "..."
}
```
- ⏰ 复盘自动化实际在 **13:56 左右**产出，早于 A股 15:00 收盘，故 `marketOverview` 为**盘中快照**（文案里会自行注明「截至14:00盘中」）。属既定配置，非故障。

### invest.json（08:35 投资自动化，2026-08-04 补记）
```json
{
  "generatedAt": "2026-08-04T12:52:26+08:00",
  "source": "daily-automation",
  "indices": [{"name":"上证指数","value":3815.59,"pct":0.16}],
  "gold": {"price":884.3,"unit":"元/克","pct":0.14,"note":"..."},
  "picks": [{"code":"300308","name":"...","market":"A股","reason":"...","suggestion":"...","risk":"..."}],
  "tip": "..."
}
```
- ⚠️ 涨跌幅字段名是 **`pct` 不是 `change`**（invest.js 全程读 `x.pct`）。巡检脚本若按 `change` 取值会全部得到 null → **误判「指数涨跌幅缺失」**，切记。
- `source` 为 `seed-default` 时是占位空壳（indices/gold 全 null），说明当日投资自动化没跑成 → 这才是真问题。

## 三、App 如何消费（关键逻辑，来自 app.js）
- `data/news.json` / `review.json` 在启动时 `fetch("data/xxx.json?v=" + APP_VERSION)`。
- `LiveData.isNewsFresh()`：检查 `generatedAt` 是否为**当天**；否则页面显示「暂未生成」。
- `review.json` 按 `generatedAt` 的日期匹配「收盘复盘」页所选日期；当天未生成时显示最近一份并标「陈旧」。
- ⚠️ `data/*.json` 带 `?v=APP_VERSION` 缓存破坏：只有 `APP_VERSION` 变化时浏览器才重新拉取；若仅更新 JSON 内容但版本号不变，需手动清缓存或用 SW 兜底。

## 四、与部署的关系（重要）
- `data/*.json` 是**静态文件**，和 `index.html`/`app.js` 一起部署在 `app.codebuddy.work`（CodeBuddy 静态托管）。
- 生成新 JSON 后，必须**重新部署**到该托管，App 才能拿到。仅改本地文件不会生效。
- 本机备用自动化生成的是**本地 JSON**，若要生效需重新部署（见运维手册「升级/部署流程」）。

## 四点五、⚠️ 部署与内容管线的冲突（2026-08-02 事故，必读）
**现象**：07:30 资讯自动化明明成功生成了当日 32 条 `news.json`，但 App 里连续两天显示的仍是 07-31 的旧资讯。
**根因**：后续任何一次部署若执行「curl 线上 data/*.json 同步到本地再部署」，会用**线上陈旧版本**覆盖掉本地刚生成的当日内容，且无备份不可恢复。08-01、08-02 两天的产出均因此丢失。
**规则**：
1. 部署前**禁止无条件回拉线上 data**。确需回拉时，先比对 `generatedAt`：**仅当线上比本地新**才覆盖；本地更新则直接部署本地版本。
2. 任何覆盖 `data/*.json` 的操作前，先 `cp data/news.json /tmp/news-backup-<date>.json`。
3. 巡检若发现 `generatedAt` 非当天、但当日资讯自动化的 memory.md 显示"已成功生成"，即可判定为本类覆盖事故，应直接重新生成而非等待管线。

## 五、接管 / 激活步骤（当云端管线失效或要升级时）
1. 确认线上云端管线已停（连续两天 `news.json` 不再刷新）。
2. 若本机 news/review 备用自动化未启用，在 WorkBuddy 自动化列表里启用。
3. 若要接管**同一个线上 URL**：需要原 CloudStudio 工作区权限（见下文「权限交接」）；否则用本机 `workbuddy_cloudstudio_deploy` 部署到**新 URL**，并把手机书签改到新地址。
4. 部署前若动了 `data/*.json`，记得同步 bump `app.js` 的 `APP_VERSION`（否则内容不刷新）。

## 六、客户端定时器（已在 app.js 内，无需继承）
PIN 锁 5 分钟无操作自动锁；本地备份每 10 分钟（IndexedDB，保留 15 份）；云端自动备份每 60 分钟下载；WebDAV `schedulePush` 自动推送坚果云。均为浏览器内运行，不依赖外部调度。

## 七、部署巡检与自动修复自动化（每日 3 次，已启用）
独立于内容管线，负责值守线上部署健康度：每天 **07:00 / 12:00 / 19:00** 各跑一次。
- 任务名：`[巡检] 工作台部署巡检与自动修复 07:00 / 12:00 / 19:00`（状态 ACTIVE）。
- 巡检内容：抓取线上首页/app.js/data/news.json 确认 200 可加载 + news.json 当日新鲜；对照本机镜像检查 `APP_VERSION`≡`CACHE_VERSION`、sw.js 注册、DOM id 一致性、data/*.json schema。
- 修复策略：发现 bug/版本错位/404 → 在本机 `pm-workbench/` 修复并同步 bump 两处版本号 → 重新部署（优先原 CloudStudio 工作区，否则 CloudStudio 部署新实例并汇报新 URL）。**仅确实修复时才部署**，避免无谓重建。
- 安全护栏：无法访问线上地址/网络受限时**不部署**，仅汇报「跳过部署」；不碰用户本地数据、不碰 2 条内容生成自动化。
- 注：CloudStudio 部署工具每次会生成**新工作区/新 URL**；若需保持同一线上地址，需原工作区更新权限。

## 八、权限交接（保持线上地址由我更新原 CloudStudio 工作区）
当前 WorkBuddy 的 CloudStudio 部署工具每次会新建工作区/新 URL。要把修复部署**固定到现有 `app.codebuddy.work` 且由我更新**，可靠路径是**把该工作区关联到 Git 仓库**：
1. 在原 CloudStudio 工作区设置里「关联代码仓库」，选 GitHub 或 工蜂(GongFeng)。
2. 在 WorkBuddy 左侧「连接器」里授权对应的 GitHub / GongFeng 连接器。
3. 之后我在本机 `pm-workbench/` 改完代码 → commit + push → CloudStudio 监听仓库自动重新部署到原 URL，地址不变。
若不想绑 Git，则保持现状：我修复后用 CloudStudio 部署新实例、给新 URL，你改书签即可。
